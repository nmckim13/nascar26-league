# Cup Team Logo Sources

The official team logo URLs are kept in `data/team-logo-sources.json`. The
NASCAR CDN blocks automated downloads for some assets, so the site should use
these only where permitted or replace them with locally supplied files. No
driver names or driver photographs are included in the BARL catalog.
